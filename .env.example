# ======================================
# GitHub Actions Secrets (optional)
# ======================================
# 작업 자동화를 위해 필요할 때만 저장소 Settings → Secrets and variables → Actions 에 추가하세요.
#   ARGOCD_SERVER=https://argocd.example.com
#   ARGOCD_TOKEN=ARGOCD_AUTH_TOKEN
#   GH_PAT=ghp_xxx (보호된 브랜치에 푸시해야 할 때)

# ======================================
# Kubernetes Sealed Secret 템플릿
# ./scripts/seal-secrets.sh 로 암호화된 매니페스트를 생성합니다.
# ======================================

# backend-secret
DATABASE_URL=postgres://helpernote:CHANGE_ME@prod-postgres:5432/helpernote
JWT_SECRET=CHANGE_ME_RANDOM_64_CHARS
COOKIE_DOMAIN=.helpernote.my

# minio-secret
MINIO_ACCESS_KEY=CHANGE_ME_MINIO_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_ME_MINIO_SECRET_KEY

# postgres-secret
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
